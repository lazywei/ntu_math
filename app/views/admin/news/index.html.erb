<h1>最新消息</h1>

<table class="table table-striped table-bordered table-condensed">
  <tr>
    <th>標題</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @admin_news.each do |admin_news| %>
  <tr>
    <td><%= admin_news.title %></td>
    <td><%= link_to '檢視', [:admin, admin_news] %></td>
    <td><%= link_to '編輯', edit_admin_news_path(admin_news) %></td>
    <td><%= link_to '刪除', admin_news, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to '新增最新消息', new_admin_news_path %>
